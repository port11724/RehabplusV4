<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointments - RehabPlus System</title>
    <link rel="icon" href="/public/images/Fav.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.css" rel="stylesheet">
    <style>
        body {
            background: #f5f7fb;
            min-height: 100vh;
        }

        .sidebar {
            min-height: 100vh;
            background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
            box-shadow: 2px 0 12px rgba(0, 0, 0, 0.08);
        }

        .sidebar .nav-link {
            color: rgba(255, 255, 255, 0.85);
            padding: 0.75rem 1rem;
            margin: 0.25rem 1rem;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            background: rgba(255, 255, 255, 0.25);
            color: #fff;
        }

        main {
            min-height: 100vh;
            padding-bottom: 4rem;
        }

        .page-header {
            background: white;
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 10px 40px rgba(82, 95, 225, 0.12);
            margin-bottom: 2rem;
        }

        .page-header h1 {
            font-weight: 700;
            color: #2d2f44;
        }

        .page-header p {
            color: #6c6f93;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            font-weight: 600;
            padding: 0.6rem 1.6rem;
            border-radius: 0.75rem;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.35);
        }

        .stat-card {
            border: none;
            border-radius: 1rem;
            box-shadow: 0 6px 20px rgba(15, 23, 42, 0.08);
            padding: 1.5rem;
            height: 100%;
        }

        .stat-card h3 {
            margin: 0;
            font-weight: 700;
            color: #2d2f44;
        }

        .stat-card small {
            color: #6c6f93;
            font-weight: 500;
        }

        .filters-card,
        .calendar-card,
        .upcoming-card {
            border: none;
            border-radius: 1.25rem;
            box-shadow: 0 8px 24px rgba(15, 23, 42, 0.08);
        }

        .filters-card .card-header,
        .upcoming-card .card-header {
            background: transparent;
            border-bottom: 1px solid rgba(102, 126, 234, 0.15);
            font-weight: 600;
            color: #2d2f44;
        }

        #calendar {
            padding: 1rem;
        }

        .fc {
            border: none;
        }

        .fc-toolbar-title {
            font-weight: 700;
            color: #2d2f44 !important;
        }

        .fc-button {
            background: #667eea !important;
            border: none !important;
            border-radius: 0.5rem !important;
            font-weight: 600;
        }

        .fc-button:hover {
            background: #5566d6 !important;
        }

        .fc-event {
            border: none;
            border-radius: 0.75rem;
            font-weight: 600;
            padding: 0.25rem 0.5rem;
        }

        .legend {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .legend-item {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(102, 126, 234, 0.08);
            color: #2d2f44;
            padding: 0.5rem 0.85rem;
            border-radius: 999px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .legend-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }

        .appointment-status-SCHEDULED { background-color: #0d6efd !important; }
        .appointment-status-COMPLETED { background-color: #198754 !important; }
        .appointment-status-CANCELLED { background-color: #6c757d !important; }
        .appointment-status-NO_SHOW { background-color: #dc3545 !important; }

        .modal-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
        }

        .modal-content {
            border: none;
            border-radius: 1.25rem;
        }

        .patient-search-result {
            cursor: pointer;
            padding: 0.75rem;
            border-radius: 0.75rem;
            transition: all 0.2s ease;
        }

        .patient-search-result:hover {
            background: rgba(102, 126, 234, 0.1);
        }

        .conflict-warning {
            border-radius: 0.75rem;
            background: rgba(255, 193, 7, 0.15);
            border: 1px solid rgba(255, 193, 7, 0.35);
            color: #856404;
            padding: 0.75rem 1rem;
            display: none;
        }

        .time-slot-btn {
            border-radius: 999px;
        }

        .list-group-item {
            border: none;
            border-bottom: 1px solid rgba(102, 126, 234, 0.08);
        }

        .list-group-item:last-child {
            border-bottom: none;
        }

        .upcoming-card .list-group-item {
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        .upcoming-card .list-group-item:hover {
            background-color: rgba(102, 126, 234, 0.08);
        }
    </style>
</head>
<body>
    <% const canManageAppointments = (user.role === 'ADMIN' || user.role === 'PT'); %>
    <div class="container-fluid">
        <div class="row">
            <%- include('partials/sidebar') %>

            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4">

                <!-- Page Header -->
                <div class="page-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="h2 mb-2">
                                <i class="bi bi-calendar-check"></i> Appointment Scheduler
                            </h1>
                            <p class="mb-0 opacity-75" style="font-size: 0.95rem;">
                                <i class="bi bi-calendar3"></i> Manage PT appointments and schedules
                            </p>
                        </div>
                        <% if (canManageAppointments) { %>
                            <button type="button" class="btn btn-light btn-lg" onclick="showBookingModal()">
                                <i class="bi bi-plus-circle"></i> New Appointment
                            </button>
                        <% } %>
                    </div>
                    <% if (canManageAppointments) { %>
                        <button type="button" class="btn btn-primary" onclick="showBookingModal()">
                            <i class="bi bi-plus-circle me-2"></i>New Appointment
                        </button>
                    <% } %>
                </div>

                <% if (!canManageAppointments) { %>
                    <div class="alert alert-info d-flex align-items-center" role="alert">
                        <i class="bi bi-eye me-2"></i>
                        <div>Clinic accounts have read-only access. You can review appointments for patients linked to your clinic.</div>
                    </div>
                <% } %>

                <div class="row g-3 mb-4">
                    <div class="col-md-4">
                        <div class="stat-card bg-white">
                            <small class="text-uppercase d-block mb-2">Today</small>
                            <h3 id="todayCount">0</h3>
                            <small><i class="bi bi-calendar-day me-1"></i>Appointments scheduled</small>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-card bg-white">
                            <small class="text-uppercase d-block mb-2">This week</small>
                            <h3 id="weekCount">0</h3>
                            <small><i class="bi bi-calendar-week me-1"></i>Total active bookings</small>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-card bg-white">
                            <small class="text-uppercase d-block mb-2">This month</small>
                            <h3 id="monthCount">0</h3>
                            <small><i class="bi bi-calendar-month me-1"></i>Sessions on the calendar</small>
                        </div>
                    </div>
                </div>

                <div class="card filters-card mb-4">
                    <div class="card-header">
                        <i class="bi bi-funnel me-2 text-primary"></i>Filter appointments
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-4">
                                <label for="filterPT" class="form-label">Physical therapist</label>
                                <select id="filterPT" class="form-select">
                                    <option value="">All therapists</option>
                                </select>
                            </div>
                            <div class="col-md-4">
                                <label for="filterClinic" class="form-label">Clinic</label>
                                <select id="filterClinic" class="form-select">
                                    <option value="">All clinics</option>
                                </select>
                            </div>
                            <div class="col-md-4">
                                <label for="filterStatus" class="form-label">Status</label>
                                <select id="filterStatus" class="form-select">
                                    <option value="">All statuses</option>
                                    <option value="SCHEDULED">Scheduled</option>
                                    <option value="COMPLETED">Completed</option>
                                    <option value="CANCELLED">Cancelled</option>
                                    <option value="NO_SHOW">No show</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row g-4">
                    <div class="col-lg-8">
                        <div class="card calendar-card mb-4">
                            <div class="card-body">
                                <div id="calendar"></div>
                            </div>
                            <div class="card-footer bg-white">
                                <div class="legend">
                                    <div class="legend-item"><span class="legend-dot" style="background:#0d6efd"></span>Scheduled</div>
                                    <div class="legend-item"><span class="legend-dot" style="background:#198754"></span>Completed</div>
                                    <div class="legend-item"><span class="legend-dot" style="background:#6c757d"></span>Cancelled</div>
                                    <div class="legend-item"><span class="legend-dot" style="background:#dc3545"></span>No show</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-4">
                        <div class="card upcoming-card h-100">
                            <div class="card-header d-flex align-items-center justify-content-between">
                                <span><i class="bi bi-clock-history me-2 text-primary"></i>Upcoming appointments</span>
                                <span class="badge rounded-pill text-bg-light" id="upcomingCount">0</span>
                            </div>
                            <div class="card-body p-0">
                                <ul class="list-group list-group-flush" id="upcomingAppointments"></ul>
                                <div id="upcomingEmptyState" class="text-center text-muted py-4">
                                    <i class="bi bi-calendar-x mb-2 d-block" style="font-size:1.75rem;"></i>
                                    No upcoming appointments in the next two weeks.
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <% if (canManageAppointments) { %>
    <div class="modal fade" id="bookingModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-xl modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="bi bi-calendar-plus me-2"></i><span id="modalTitle">New appointment</span></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="conflict-warning" id="conflictWarning">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        <span id="conflictMessage"></span>
                    </div>

                    <form id="appointmentForm">
                        <input type="hidden" id="selectedPatientId">
                        <div class="row g-4">
                            <div class="col-md-6">
                                <h6 class="fw-semibold text-primary mb-3"><i class="bi bi-person-circle me-2"></i>Patient</h6>
                                <div class="mb-3">
                                    <label for="patientSearch" class="form-label">Search patient <span class="text-danger">*</span></label>
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="bi bi-search"></i></span>
                                        <input type="text" class="form-control" id="patientSearch" placeholder="HN, PT number, or name" autocomplete="off">
                                        <button type="button" class="btn btn-primary" onclick="searchPatients()">Search</button>
                                    </div>
                                    <div id="patientSearchResults" class="border rounded-3 mt-2 p-2" style="display:none; max-height:240px; overflow-y:auto;"></div>
                                    <div id="selectedPatientInfo" class="alert alert-success mt-2" style="display:none;">
                                        <i class="bi bi-check-circle me-2"></i>
                                        <strong>Selected:</strong> <span id="selectedPatientDisplay"></span>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label for="appointmentPT" class="form-label">Physical therapist <span class="text-danger">*</span></label>
                                    <select id="appointmentPT" class="form-select" onchange="checkConflicts()" required>
                                        <option value="">Select therapist</option>
                                    </select>
                                </div>

                                <div class="mb-3">
                                    <label for="appointmentClinic" class="form-label">Clinic <span class="text-danger">*</span></label>
                                    <select id="appointmentClinic" class="form-select" required>
                                        <option value="">Select clinic</option>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <h6 class="fw-semibold text-primary mb-3"><i class="bi bi-clock me-2"></i>Schedule</h6>
                                <div class="mb-3">
                                    <label for="appointmentDate" class="form-label">Date <span class="text-danger">*</span></label>
                                    <input type="date" class="form-control" id="appointmentDate" onchange="checkConflicts()" required>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label for="appointmentStartTime" class="form-label">Start time <span class="text-danger">*</span></label>
                                        <input type="time" class="form-control" id="appointmentStartTime" onchange="checkConflicts()" required>
                                    </div>
                                    <div class="col-md-6">
                                        <label for="appointmentEndTime" class="form-label">End time <span class="text-danger">*</span></label>
                                        <input type="time" class="form-control" id="appointmentEndTime" onchange="checkConflicts()" required>
                                    </div>
                                </div>
                                <div class="mt-3">
                                    <small class="text-muted d-block mb-2">Quick duration</small>
                                    <div class="d-flex flex-wrap gap-2">
                                        <button type="button" class="btn btn-outline-primary btn-sm time-slot-btn" onclick="setDuration(30)">30 min</button>
                                        <button type="button" class="btn btn-outline-primary btn-sm time-slot-btn" onclick="setDuration(45)">45 min</button>
                                        <button type="button" class="btn btn-outline-primary btn-sm time-slot-btn" onclick="setDuration(60)">1 hour</button>
                                        <button type="button" class="btn btn-outline-primary btn-sm time-slot-btn" onclick="setDuration(90)">1.5 hours</button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <hr class="my-4">

                        <h6 class="fw-semibold text-primary mb-3"><i class="bi bi-card-text me-2"></i>Details</h6>
                        <div class="row g-3">
                            <div class="col-md-4">
                                <label for="appointmentType" class="form-label">Appointment type</label>
                                <select id="appointmentType" class="form-select">
                                    <option value="">Select type</option>
                                    <option value="Initial Assessment">Initial assessment</option>
                                    <option value="Follow-up">Follow-up</option>
                                    <option value="Treatment Session">Treatment session</option>
                                    <option value="Re-evaluation">Re-evaluation</option>
                                    <option value="Discharge">Discharge</option>
                                </select>
                            </div>
                            <div class="col-md-8">
                                <label for="appointmentReason" class="form-label">Reason for visit</label>
                                <textarea id="appointmentReason" class="form-control" rows="2" placeholder="e.g. Post-surgery rehabilitation"></textarea>
                            </div>
                            <div class="col-12">
                                <label for="appointmentNotes" class="form-label">Internal notes</label>
                                <textarea id="appointmentNotes" class="form-control" rows="2" placeholder="Additional context for the therapist"></textarea>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-2"></i>Close
                    </button>
                    <button type="button" class="btn btn-primary" onclick="saveAppointment()">
                        <i class="bi bi-check-circle me-2"></i>Save appointment
                    </button>
                </div>
            </div>
        </div>
    </div>
    <% } %>

    <div class="modal fade" id="viewAppointmentModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header bg-info text-white">
                    <h5 class="modal-title"><i class="bi bi-calendar-event me-2"></i>Appointment details</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="appointmentDetails"></div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle"></i> Close
                    </button>
                    <% if (canManageAppointments) { %>
                        <button type="button" class="btn btn-warning" onclick="rescheduleAppointment()">
                            <i class="bi bi-calendar-x"></i> Reschedule
                        </button>
                        <button type="button" class="btn btn-success" onclick="markAsCompleted()">
                            <i class="bi bi-check-circle"></i> Mark Completed
                        </button>
                        <button type="button" class="btn btn-danger" onclick="cancelAppointment()">
                            <i class="bi bi-x-octagon"></i> Cancel
                        </button>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/moment@2.29.4/moment.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.js"></script>
    <script>
        window.userInfo = {
            id: <%= user.id %>,
            role: <%- JSON.stringify(user.role) %>,
            email: <%- JSON.stringify(user.email || '') %>
        };
    </script>
    <script src="/public/js/appointments.js?v=<%= Date.now() %>"></script>
</body>
</html>
